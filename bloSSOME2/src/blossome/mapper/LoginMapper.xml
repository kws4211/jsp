<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blossom.mapper.LoginMapper">

<select id="LoginView" resultType="blossome.vo.LoginVO">
	SELECT *
	FROM BLO_MEMBER m
	WHERE MEM_ID = #{MEMID}	
</select>

<insert id="insertLogin">
	INSERT INTO 
	VALUES
</insert>

</mapper>

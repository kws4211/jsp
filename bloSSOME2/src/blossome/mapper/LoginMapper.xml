<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blossom.mapper.LoginMapper">

<select id="checklogin" parameterType="hashMap" resultType="int">
	SELECT count(*) as count
	FROM BLO_MEMBER
	WHERE MEM_ID = #{id} and mem_pass = #{pw}
</select>

<select id="searchfirst" parameterType="hashMap" resultType="blossome.vo.LoginVO">
	SELECT mem_question, mem_answer
	FROM BLO_MEMBER
	WHERE mem_name = #{name} and mem_email = #{email}
</select>
</mapper>
